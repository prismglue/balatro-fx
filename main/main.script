function init(self)
	print("main loaded")
	print(go.get_id())
	--msg.post(".", "acquire_input_focus")
	--msg.post("@render:", "use_fixed_fit_projection", { near = -1, far = 1 })
end

function on_message(self, msg_id, message, sender)
	-- print(msg_id)
	-- pprint(message)
	-- if msg_id == hash("touch") then
		-- print("Touch!")
		print(go.get_position("/card"))
	-- end
end
